export enum ColumnType {
  bigint = 'BIGINT',
  boolean = 'BOOLEAN',
  bytea = 'BYTEA',
  date = 'DATE',
  decimal = `DECIMAL(22, 6)`,
  geometryPoint = 'geometry(Point)',
  integer = 'INTEGER',
  jsonb = 'jsonb',
  time = 'TIME WITHOUT TIME ZONE',
  timeStamp = 'TIMESTAMP',
  uuid = 'UUID',
  varchar = 'VARCHAR',
}
